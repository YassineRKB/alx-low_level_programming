# include "main.h"
/**
* _memset - function that fills memory with a constant byte.
* @s: pointer to targeted memory area
* @b: constant byte
* @n: n bytes
* Return: pointer to the memory area s.
**/
char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
